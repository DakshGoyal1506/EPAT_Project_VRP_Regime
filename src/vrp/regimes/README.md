# Regimes

This package contains regime-detection logic, including threshold filters, Gaussian HMMs, and autoregressive regime models.

Regime decisions must remain timestamp-safe and should not use future information in live features.
