# Strategies

This package contains the regime-conditioned and unconditional strategy logic used by the backtest layer.

Strategy code should consume validated features and forecasts from `src/vrp/features/`, `src/vrp/forecasting/`, and `src/vrp/regimes/`.
