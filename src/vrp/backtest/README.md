# Backtest

This package contains the vectorised backtest engine, accounting helpers, and performance diagnostics used by the strategy layer.

The backtest code must respect the no-lookahead contract and use only signals available at decision time.
