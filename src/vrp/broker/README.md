# Broker

This package contains the paper-signal boundary for the broker layer.

It is intentionally separated from live trading. The broker code should only support guarded paper workflows, cached inputs, and explicit no-live-trading defaults.
